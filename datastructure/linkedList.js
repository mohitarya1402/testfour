function llNode(data,next)
{
    this.data = data;
    this.next = next;
}
function insertNode(val)
{
    const node = new llNode(val)
    if(node.next==null){
    return node;
    }
    let current = 
}